security:
  user:
    name: test
    password: test
  manager:
    name: manager
    password: manager

spring:
  datasource:
    url: jdbc:h2:mem:lms;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  mail:
    host: localhost
    port: 3025
    username: test
    password: test
    from: noreply@lms.test.com

course:
  reminder:
    cron: "0 0 0 1 1 ?"
